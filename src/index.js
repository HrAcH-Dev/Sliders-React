import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './Slider2/App';
import "swiper/css/bundle";


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
